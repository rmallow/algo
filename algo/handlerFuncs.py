"""
how this works might end up being changed
not sure if handler can just use a func???
prob needs its own data store and a func

could be a calculator class that stores the data and calculates
"""

import algo.message

def testFunc1(message):
    print(message.m_message)